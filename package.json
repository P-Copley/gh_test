{
  "name": "postgres",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup-db": "psql -f db/setup-db.sql",
    "seed": "PGDATABASE=nc_snacks node db/run-seed.js",
    "query-test": "psql -f query-test.sql > output.txt"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "pg-format": "^1.0.4"
  }
}
